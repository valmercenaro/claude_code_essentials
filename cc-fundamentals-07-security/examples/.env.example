# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit your .env file to git!
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual keys
# =============================================================================

# -----------------------------------------------------------------------------
# AI / LLM APIs
# -----------------------------------------------------------------------------
# Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI (GPT models)
OPENAI_API_KEY=sk-your-key-here

# Google (Gemini)
GOOGLE_API_KEY=your-key-here

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# Or for SQLite:
# DATABASE_URL=sqlite:///./app.db

# Redis (if used)
REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------------------------
# Cloud Services
# -----------------------------------------------------------------------------
# AWS (if needed)
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=your-secret-here
AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# Third-Party Services
# -----------------------------------------------------------------------------
# Stripe (payments)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...

# SendGrid / Email
SENDGRID_API_KEY=SG.your-key-here

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Environment: development, staging, production
NODE_ENV=development

# Server
PORT=3000
HOST=localhost

# Security
JWT_SECRET=generate-a-random-32-char-string
SESSION_SECRET=another-random-string

# -----------------------------------------------------------------------------
# IMPORTANT SECURITY NOTES
# -----------------------------------------------------------------------------
# 1. This file should NEVER contain real values
# 2. Real values go in .env (which is gitignored)
# 3. Use strong, unique values for each secret
# 4. Rotate keys regularly, especially after any exposure
# 5. Use different keys for development vs production
